document.addEventListener('DOMContentLoaded',()=>{const navLinks=document.querySelectorAll('nav ul li a, .nav-links-mobile li a');const mainContent=document.querySelector('main');const loadPage=async(url)=>{try{const response=await fetch(url);if(!response.ok){throw new Error(`A página não carregou, deu erro`)}
const pageHtml=await response.text();const parser=new DOMParser();const doc=parser.parseFromString(pageHtml,'text/html');const newMainContent=doc.querySelector('main').innerHTML;mainContent.innerHTML=newMainContent;document.title=doc.title;const newHeading=mainContent.querySelector('h1');if(newHeading){newHeading.setAttribute('tabindex','-1');newHeading.focus()}}catch(error){console.error('Deu ruim no SPA:',error);window.location.href=url}};navLinks.forEach(link=>{link.addEventListener('click',(event)=>{event.preventDefault();const href=link.getAttribute('href');loadPage(href);window.history.pushState(null,'',href);const mobileMenu=document.getElementById('nav-links-mobile');mobileMenu.classList.remove('ativo')})});window.addEventListener('popstate',()=>{loadPage(window.location.pathname)})})